Big trust statistic — Anton display numeral over an uppercase label. Use in the "confianza" band.

```jsx
<StatCard icon={<CalendarIcon />} value="+15" label="años de experiencia" />
<StatCard value="+500" label="clientes" />
```
